import { WebWidgetCore } from '../webwidgetcore-utils';
export declare class Container {
    private webWindow;
    private containerId;
    private iframeId;
    private get closeBtnId();
    constructor({ containerId, iframeId, webWindow, webWidget, }: {
        containerId: string;
        iframeId: string;
        webWindow: Window;
        webWidget: WebWidgetCore;
    });
    get window(): Window;
    get containerElement(): HTMLDivElement | null;
    get iframeSelector(): string;
    show: (withCloseBtn?: boolean) => void;
    hide: () => void;
}
