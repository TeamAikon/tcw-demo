import { WebWidgetCore } from '../webwidgetcore-utils';
export declare const createHtmlContainer: ({ webWindow, containerId, closeBtnId, iframeId, webWidget, hideContainer, }: {
    webWindow: Window;
    containerId: string;
    iframeId: string;
    closeBtnId: string;
    webWidget: WebWidgetCore;
    hideContainer: () => void;
}) => void;
