import { OreId, WebWidgetActionResult, WebWidgetProps } from 'oreid-js';
import { Container } from '../Container';
import { WebWidgetCore } from '../webwidgetcore-utils';
export declare class WebWidgetCoreHandler {
    private oreId;
    private webWidget;
    private container;
    private isBusy;
    private _widgetProps?;
    private willClose;
    constructor({ oreId, webWidget, container }: {
        oreId: OreId;
        webWidget: WebWidgetCore;
        container: Container;
    });
    private willCloseListener;
    private open;
    private close;
    private getWebWidgetCoreProps;
    render<T extends WebWidgetActionResult>(webWidgetActionProps: NonNullable<WebWidgetProps['action']>): Promise<T>;
}
