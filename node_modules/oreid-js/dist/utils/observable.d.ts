export declare class Observable<Subscriber extends Function> {
    private _subscribers;
    constructor();
    subscribe(subscriber: Subscriber): void;
    unsubscribe(subscriber: Subscriber): void;
    callSubscribers(): void;
}
