import OreIdContext from '../../core/IOreidContext';
import { AuthProvider } from '../../models';
import { ApiMessageResult } from '../models';
export declare type ApiPasswordLessVerifyCodeParams = {
    code?: string;
    email?: string;
    phone?: string;
    provider: AuthProvider;
};
/**
 *  Call api account/login-passwordless-verify-code
 *  Returns { success: true } if code is verified, throws if code/verification fails
 * */
export declare function callApiPasswordLessVerifyCode(oreIdContext: OreIdContext, params: ApiPasswordLessVerifyCodeParams): Promise<ApiMessageResult>;
